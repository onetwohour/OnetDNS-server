# CoreDNS 벤치 설정. 벤치 워크디렉터리(BENCHDIR)로 복사해 쓴다.
# 캐시 용량은 onetdns-cache.toml의 cache_size와 같은 값이어야 한다.
.:15358 {
    cache {
        success 20000 3600 0
        denial 5000 3600 0
    }
    forward . 127.0.0.1:15400 {
        max_concurrent 10000
    }
}
