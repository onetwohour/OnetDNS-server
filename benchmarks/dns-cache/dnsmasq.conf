# 벤치 워크디렉터리(BENCHDIR)로 복사해 쓴다. 캐시 용량은 onetdns-cache.toml의
# cache_size와 **같은 값이어야 한다** — 다르면 처리량·RSS 비교가 엔진 차이가 아니라
# 설정 차이를 재게 된다.
port=15356
listen-address=127.0.0.1
bind-interfaces
no-resolv
no-hosts
server=127.0.0.1#15400
cache-size=20000
